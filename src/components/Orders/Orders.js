import React from 'react';

const Orders = () => {
    return (
        <div>
            <h1>This is Order Page</h1>
            <h2>You con't get access without login</h2>
        </div>
    );
};

export default Orders;